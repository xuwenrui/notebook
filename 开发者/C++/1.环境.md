##### 1. 安装 C++ 编译器
- Visual Studio（推荐）
- 下载：  
    👉 [Visual Studio Community](https://visualstudio.microsoft.com/vs/community/)
- 安装时选择：
    - 「**使用 C++ 的桌面开发**」（Desktop development with C++）
- 它会自动安装：
    - MSVC 编译器（cl.exe）
    - Windows SDK
    - CMake 工具
    - Ninja / MSBuild 等构建工具
> ✅ 优点：和 Qt、CMake 无缝集成，兼容性最好。  
> ⚠️ 注意：安装后命令行要用 “x64 Native Tools Command Prompt for VS”。

|Java 特性|C++ 对应与差异|
|---|---|
|自动垃圾回收|手动管理内存（或使用智能指针 `std::unique_ptr` / `std::shared_ptr`）|
|类继承单一根 `Object`|C++ 多继承、没有统一基类|
|异常 Exception|C++ 异常成本高，很多库用返回值代替|
|Stream API / Lambda|C++11+ 有 `lambda`、`range-based for`、`std::vector`、`std::thread` 等|
|包（package）|使用 `namespace` 组织代码|
|JVM|C++ 编译为原生可执行文件（不依赖虚拟机）|

#### 确认环境
1️⃣ 打开「开始菜单」  
→ 搜索 **x64 Native Tools Command Prompt for VS 2022**  
→ 打开后输入：
`cl`
若输出类似：
`Microsoft (R) C/C++ Optimizing Compiler Version 19.3xxx for x64`

2️⃣ 检查 CMake：
`cmake --version`
d:\Program Files\Microsoft Visual Studio\2022\Community>cmake --version
cmake version 3.31.6-msvc6