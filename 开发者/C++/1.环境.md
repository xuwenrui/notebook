##### 1. 安装 C++ 编译器
- Visual Studio（推荐）
- 下载：  
    👉 [Visual Studio Community](https://visualstudio.microsoft.com/vs/community/)
- 安装时选择：
    - 「**使用 C++ 的桌面开发**」（Desktop development with C++）
- 它会自动安装：
    - MSVC 编译器（cl.exe）
    - Windows SDK
    - CMake 工具
    - Ninja / MSBuild 等构建工具
> ✅ 优点：和 Qt、CMake 无缝集成，兼容性最好。  
> ⚠️ 注意：安装后命令行要用 “x64 Native Tools Command Prompt for VS”。

##### 2. 安装 CMake（构建系统）
- 下载：[https://cmake.org/download/](https://cmake.org/download/)
- 安装时勾选 “Add CMake to PATH”
- 验证：
    `cmake --version`
```shell
C:\Users\38627>cmake --version
cmake version 4.2.0-rc2
```

