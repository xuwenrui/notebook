### 注册
访问 [npmjs.com/signup](https://www.npmjs.com/signup) 注册账号
![[Pasted image 20250706161409.png|400]]
paw：dn..



注意：
	npm ls react //检查依赖里包版本


#### 实现
main-app
```javascript
import logo from "./logo.svg";
import "./App.css";
import MyButton, { Add } from "@frankxu/hotpot-react-lib-demo";

function App() {
  const handleClick = () => {
    const res = Add(2, 3);
    alert("Button clicked! add result:" + res);
  };
  return (
    <div className="App">
      <header className="App-header">
        <img src={logo} className="App-logo" alt="logo" />
        <p>
          Edit <code>src/App.js</code> and save to reload.
        </p>
        <a
          className="App-link"
          href="https://reactjs.org"
          target="_blank"
          rel="noopener noreferrer"
        >
          Learn React
        </a>
        <MyButton label="Click Me" onClick={handleClick} variant="primary" />
      </header>
    </div>
  );
}

export default App;

```

hotpot-react-lib-demo
MyButton.jsx
```
import React from "react";

const MyButton = ({ label, onClick, variant = "primary" }) => {
  return (
    <button className={`my-button ${variant}`} onClick={onClick}>
      {label}
    </button>
  );
};

const Add = (a, b) => {
  return a + b;
};
export default MyButton;
export { Add };

```

本地安装 npm i ../hotpot-react-lib-demo/
package.json
    "@frankxu/hotpot-react-lib-demo": "@frankxu/hotpot-react-lib-demo",


#### 部署
添加.npmignore
```
# .npmignore
/node_modules
/package-lock.json
/vite.config.js
/src
README.md
.git
```
查看镜像
npm config get registry
阿里镜像 https://registry.npmmirror.com/
官方源：https://registry.npmjs.org/

设置成官方镜像
npm config set registry https://registry.npmjs.org/
npm config set registry https://registry.npmmirror.com/
npm login
npm publish
第一次使用：npm publish --access public
![[Pasted image 20250706201117.png|475]]


